# Copy this file to ".env" and fill in your real MySQL credentials.
# .env is gitignored so your password is never pushed.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=washwise
